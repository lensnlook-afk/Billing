import { useEffect, useState } from 'react'; import { api } from '../api/client';
const money=(value:string)=>new Intl.NumberFormat('en-IN',{style:'currency',currency:'INR'}).format(Number(value));
export function Dashboard(){const [data,setData]=useState<{today:{sales:string;collections:string;outstanding:string;invoices:string};lowStock:number}|null>(null);const [error,setError]=useState('');useEffect(()=>{api.dashboard().then(setData).catch(e=>setError(e.message))},[]);if(error)return <section className="empty"><h1>Dashboard unavailable</h1><p>{error}</p></section>;if(!data)return <section className="empty">Loading today’s operations…</section>;const cards=[['Sales today',money(data.today.sales)],['Collected today',money(data.today.collections)],['Outstanding',money(data.today.outstanding)],['Invoices',data.today.invoices],['Low-stock variants',String(data.lowStock)]];return <section className="dashboard"><div className="metric-grid">{cards.map(([label,value])=><article className="metric" key={label}><span>{label}</span><strong>{value}</strong><small>Live data from posted invoices</small></article>)}</div><article className="info-card"><p className="eyebrow">CONTROL POINT</p><h3>Counter-safe by design</h3><p>Sales are only posted after the invoice number, payment records, inventory ledger, balance projection, and tamper-evident audit record commit together.</p></article></section>}
