import { useState } from 'react'; import { api, type User } from '../api/client';
export function Login({onLogin}:{onLogin:(user:User)=>void}){const [identifier,setIdentifier]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);const backdrop=`linear-gradient(90deg,rgba(10,26,29,.89) 0%,rgba(10,26,29,.67) 49%,rgba(10,26,29,.28) 100%),url(${import.meta.env.BASE_URL}lens-look-storefront.png)`;return <main className="login" style={{backgroundImage:backdrop}}><section><div className="brand"><i>◉</i><span>Lens&Look <small>OPTICAL</small></span></div><p className="eyebrow hero-eyebrow">NOW IN BADAMI &amp; ILKAL</p><h1>Every look<br/>starts with vision.</h1><p>Sign in to bill, receive stock, and serve customers. Every sensitive operation is recorded.</p></section><form onSubmit={async e=>{e.preventDefault();setBusy(true);setError('');try{onLogin((await api.login(identifier,password)).user)}catch(err){setError(err instanceof Error?err.message:'Could not sign in')}finally{setBusy(false)}}}><p className="form-kicker">ADMIN &amp; EMPLOYEE ACCESS</p><label>Login as<input autoComplete="username" value={identifier} onChange={e=>setIdentifier(e.target.value)} placeholder="Admin or Employee" required/></label><label>Password<input autoComplete="current-password" type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<p className="error">{error}</p>}<button className="primary" disabled={busy}>{busy?'Signing in…':'Sign in securely'}</button><p className="muted">Use your assigned Admin or Employee account.</p></form></main>}
