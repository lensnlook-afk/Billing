import { readdir, readFile } from 'node:fs/promises'; import { join } from 'node:path'; import { pool, transaction } from './lib/db.js';
const migrationDir = new URL('../migrations/', import.meta.url);
await transaction(async db => { await db.query('CREATE TABLE IF NOT EXISTS schema_migrations (name text PRIMARY KEY, applied_at timestamptz NOT NULL DEFAULT now())'); const applied = new Set((await db.query('SELECT name FROM schema_migrations')).rows.map(r => r.name)); for (const name of (await readdir(migrationDir)).filter(n => n.endsWith('.sql')).sort()) { if (!applied.has(name)) { console.log(`Applying ${name}`); await db.query(await readFile(join(migrationDir.pathname, name), 'utf8')); await db.query('INSERT INTO schema_migrations(name) VALUES($1)', [name]); } } }); await pool.end();
