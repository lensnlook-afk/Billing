{
  "installCommand": "npm install --include=dev --workspaces",
  "buildCommand": "npm run build --workspace=@clarity/web",
  "outputDirectory": "apps/web/dist",
  "framework": null,
  "functions": {
    "api/[...path].ts": {
      "maxDuration": 30
    }
  }
}
